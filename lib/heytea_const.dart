import 'package:flutter/material.dart';

class HeyTeaConst {

  static const toastAnimationDuration = Duration(milliseconds: 300);
  static const toastBackgroundColor = Colors.black54;
  static const toastDuration = Duration(seconds: 2);
  static const toastIconSize = 66.0;
}